import React from "react";

const Header = () => {
  return (
    <div className="w-full flex bg-[#f0efeb] justify-between items-center text-[10px] md:text-xs font-semibold tracking-[0.2em] py-10 px-6 uppercase ">
        <span>Create Post</span>
        <span>Menu</span>
      </div>
  );
};

export default Header;
